{
  "name": "serverless-service-config-plugin",
  "version": "0.1.1",
  "description": "Retrieve service configs and secrets from Consul and Vault",
  "main": "src/index.js",
  "scripts": {
    "test": "tape ./src/utils/*.test.js ./src/*.test.js | tap-summary",
    "ci": "npm install && npm run lint && npm test",
    "release": "rm -rf node_modules && npm i --production && npm publish",
    "lint": "eslint src",
    "lint:fix": "eslint --fix src"
  },
  "author": "Wealth Wizards Engineering",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1127.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "request": "^2.88.2",
    "request-promise-native": "^1.0.9"
  },
  "devDependencies": {
    "@wealthizardsengineering/tap-summary": "git+https://github.com/WealthWizardsEngineering/tap-summary.git",
    "escope": "^3.6.0",
    "eslint": "^8.2.0",
    "eslint-plugin-import": "^2.25.2",
    "proxyquire": "^2.1.3",
    "sinon": "^9.2.4",
    "tape": "^5.5.3"
  }
}
